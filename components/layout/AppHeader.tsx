/**
 * Minimal dashboard header — just the brand wordmark. The personal
 * greeting and date live inside the daily reading hero card now, so
 * we don't repeat them here.
 */
export function AppHeader() {
  return (
    <header className="pt-2">
      <h1 className="font-serif text-2xl font-semibold tracking-tight">Supernova</h1>
    </header>
  );
}
